# Changelog

AIQuad 的版本记录，也是**发布说明的唯一源头**。GitHub Releases 上的更新说明由这里派生：

```bash
node scripts/release-notes.js          # 抽 package.json 里的版本，写 build/release-notes-<版本>.md
gh release create v0.4.9 ... --notes-file build/release-notes-0.4.9.md
```

脚本把条目标题渲染成发布页惯用的 `## AIQuad v<版本>`（沿用 0.4.2 ~ 0.4.9 四份 Release 的既有样式），
再拼上 [`docs/release-boilerplate.md`](../docs/release-boilerplate.md) 那段固定尾巴
（安装 / 已知限制 / 许可证），**抽不到这一版的段落就直接退出**——版本没写进这里就发不出去。
发版后 `node scripts/post-release-check.js` 会回查远端，确认发布页的说明确实来自这一段。

条目写法 `## [版本] - 日期`。0.4.5 与 0.4.7 是内部过程版本、没有单独发版，内容并入 0.4.6 条目。
0.4.2 ~ 0.4.9 四段正文是从发布页既有说明回填的，与线上逐字一致（固定尾巴那几段没有回填，它们只在下面那份
boilerplate 里维护）。

**发布说明按「累计」写**（2026-09-15 起，规则见 [`docs/release-notes.md`](../docs/release-notes.md)）：
发布页不逐版罗列，而是把**上一个已发布版本之后**的所有版本合并成一篇 ——
某个版本加了功能、下一个版本又修了它，在发布页上只留一句"加了什么"。
所以**最新那一版的条目 = 这一段区间的累计摘要**（合并、去重、按事归类），
被并进来的旧版本条目原样留着（那是开发侧的完整履历，也是本摘要的取材来源）。
取材：`node scripts/release-notes.js --since auto`（把区间内的旧条目原样倒到 `build/release-draft-<版本>.md`），
合并写回本条目后再 `node scripts/release-notes.js <版本>` 生成发布页说明。

## [0.4.11] - 2026-09-15

这一版做的是"看得见的地方重做一遍"：**设置页整个重排**，以及**修掉两个一直定位不准的分格显示问题**——
下排分格被裁掉的那截浏览器外壳会漏到上排分格上，点分格时顶栏底下会闪一条白线。

> 本版说明**合并了 0.4.10 与 0.4.11 两版的全部改动**（0.4.10 没有单独发布）。

### 一、设置页重做

**一行一件事。** 全页 20 行、16 个 `ⓘ`：行里只留「名字 + 当前值 + 控件」，
原先跟在控件后面那些说明句子全部收进标签旁那个 13px 的 `ⓘ` 里（悬浮才显示）。
老设置页的毛病是"越写越长"——每个开关后面跟一句解释，一行变两行、两行变三行，滚三屏才找到那个开关。
而设置页**一辈子只被认真读一次**，就是找控件的那次，所以行底下每多一句话，都只是把下一个控件往下推。
需要一整句话的进 `ⓘ`；属于整组说明的（比如"隐藏只是从选择器里摘掉"）放卡片标题右侧那行小字，不占正文行。

- **窗口 920 → 800px**，卡片走两列网格，需要整宽的跨两列（面板与呼出 / AI 服务 / 数据与缓存 / 关于）。
  一行行铺满整个宽度的话，控件会离它自己的标签好几百像素远。
- **行布局改成「定宽标签列 138px + 控件紧跟其后」**。原来是标签靠左、控件推到最右，窗口一宽，
  同一行的标签和它的开关能隔开四百多像素；定宽之后所有开关落在同一条竖线上，扫一列就够。
  「面板宽度」的读数因此挪到滑块右边，并且**只用百分比、不再换算像素**——
  不同机器的工作区宽度不一样，那个像素值没法横向比较。
- **「代理设置」挪到「面板与呼出」正下方**，与「浏览器内核」并排；两张半宽卡**等高**，行的位置逐条对齐。
  「全局代理」「服务器」两行原先各折成两行（控件列被自身的 `flex-basis: auto` 按内容估宽顶出行宽，
  被 `flex-wrap` 整条挤到标签下面，行高 39px 变 65px），现在各自只占一行 ——
  两卡的自然高度差从 54px 收到 3px，剩下的由 `stretch` 抹平。
- **AI 服务列表限高 300px、表头吸顶**。条目数没有上限，不封的话加满 30 个 AI 整页要滚四屏，
  上面那几张卡全被顶出视野。
- **AI 服务表的输入框修掉了「白块」**：这些框是 `settings.js` 用 `document.createElement('input')`
  建的，**没有 `type` 属性**，而 CSS 属性选择器只认 DOM 上真实存在的属性 —— `input[type="text"]`
  那整条规则从来没有命中过它们，几个框一直用系统的白底 + 灰边。现在改成极淡底 + 圆角
  （一眼看得出能改），悬停加深、聚焦才亮蓝边；名称走正文亮度、网址走等宽小字，五列各有分工。
- **隐藏的条目不再划删除线**。那条线画在输入框里的文字上，名字和网址糊成一片；
  改成整行压暗（悬停提亮）+ 名称格左侧一条竖标，一个字都不动。
- **标题栏改为自绘**。Windows 的**非激活**标题栏会被系统换一套配色重绘，而设置页是深色的 ——
  鼠标一离开窗口，顶上就白一条。现在摘掉系统标题栏，页面里那条头部自己当标题栏
  （`titleBarStyle: hidden` + `titleBarOverlay` 保留最小化/最大化/关闭三个系统按钮），
  颜色只由 CSS 决定，失焦不再变脸（屏幕采样实测：激活与失焦两种状态下标题栏像素**逐点相同**）。
  标题栏下沿那条 1px 分割线也已贯通到右上角：三个系统按钮的叠加层与标题栏等高时，
  它的底边正好压住分割线所在的那行像素，线画到按钮下面会整段消失；
  把叠加层高度取「标题栏高度 − 2」即可（本次一并修掉）。
- **头部常驻保存回执**：全页自动保存、没有"保存"按钮，那条"已自动保存"就是唯一的落盘凭据，
  从页面底部挪到标题右侧；标题旁加了版本胶囊。
- **「X 个快捷键已生效」挂在「面板与呼出」卡片的标题行右端**。它说的是整张卡四处快捷键
  （呼出 + 1 / 2 / 4 格）加起来有几个真注册上了，跟在任何一行下面都会被当成那一行的附属；
  现在是标题行右侧那一小行字，为空时整条收掉；谁跟别的程序冲突了，就把那个组合键写在这儿，红色。
- 开关改画成 iOS 那种滑块，停靠位置 / 宽度档位改成分段按钮；行分隔只在行与行之间画一条细线；
  「闲置分格自动清理」关掉时分钟框跟着压暗并禁用（还能改、改了没用的输入框比灰掉的更让人困惑）；
  窗口拖窄（<720px）自动退回单列并把表格里几个定宽列一起收窄。

**换了应用图标。** 新图标是"四格悬浮"：2×2 圆角格、纯图形透明底，三格主蓝 `#3B82F6`、
右下一格浅蓝 `#93C5FD` —— 对应面板的分格布局。安装包 / exe / 任务栏 / 托盘 / 设置页顶栏一起换新
（生成脚本 `scripts/make-icons.py`，纯几何绘制 + 4 倍超采样）。

`settings.js` 的读写链路（`collect()` / `save()` / 快捷键抓取）一行没改，所有 `id`、类名与
`#ai-rows` 结构原样保留，`scripts/verify-settings.js`、`verify-settings-extra.js`、
`verify-shortcut-capture.js` 三条回归的判据全部照旧成立。设计取舍（含"提示用原生 title、
样式跟不了主题、触屏点不出来"这几个已知代价）记在 [`docs/settings-panel.md`](https://github.com/WW-Ares/AIQuad/blob/main/docs/settings-panel.md)。

### 二、分格里被裁掉的浏览器外壳不再入侵邻格

2 格 / 4 格布局下选中下面那格，它会有一截"入侵"到上面那格的底部。

**那截"被裁掉的浏览器外壳"其实有两层，都会漏到相邻分格上，而且两层都是 DWM 画的、都不受 `SetWindowRgn` 约束。**
分格里的浏览器是标准窗口，自带约 96px 的标题栏 + 标签栏 + 地址栏，我们靠 `SetWindowRgn` 把这一截从可视区裁掉，问题出在 Win11：

1. **系统背板（Mica）**：DWM 会给窗口铺一层背景，它照旧铺满整窗。Chrome 自己的内容确实被裁掉了，
   那一截却变成一条死板纯色（跟着壁纸走，浅色壁纸下是奶白 `#F9F1EB`），照样压在邻居上面。
2. **原生窗口框架**：窗口样式里的标题栏位还会让 DWM 在窗口矩形边上画一圈 1px 外框线和右上角一个 ✕。
   它面积很小，但同样越过了裁剪区域，在邻格上表现为"一条横线和一个 ✕"。

这个 bug 之前几次都没修掉，是因为**它看不见**：`GetWindowRgn` / `PtInRegion` 读回来一切正常
（区域确实设上了，命中测试也确实生效），所有基于区域的自检都判"通过"，而以前的自检全在读区域——
**只有采屏幕像素才看得见**。之前还一直以为"本机是 Win10 + 软件渲染所以复现不出来"，
其实本机就是 Win11 24H2（build 26100），能复现。

修法是两步，代价都约等于零：关掉系统背板（`DwmSetWindowAttribute` + `DWMWA_SYSTEMBACKDROP_TYPE = NONE`），
以及摘掉窗口样式里的 `WS_CAPTION | WS_THICKFRAME`。除了建窗口和每次定位时各确认一次，
30ms 几何看门狗也加了三项核对——Chrome 在激活 / 失活 / 换皮肤 / 刚显示出来时会把这些重新挂回来，
而那时候几何往往纹丝不动，光靠"位置变了才修"兜不住。

（样式位是逐个实测定下来的：单独摘 `WS_THICKFRAME` 一点用没有，单独摘 `WS_CAPTION` 或 `WS_SYSMENU` 只减一半，
两个一起摘才干净；摘掉之后窗口的外壳厚度会从 `{8,96,7,7}` 变成 `{6,96,7,6}`，看门狗会按新厚度重新定位。）

### 三、点分格时顶栏底下闪一下的那条白线

1 格 / 2 格（上面那格）/ 4 格（上排那两格）都一样：点一下，顶栏底边会冒出一条网页的白线，一两帧就没了。

原因是顶栏那一排的网页**故意**往上出血、压在顶栏底下（`--bleed-top`，为了顶栏和网页之间不留缝），
而点分格时系统会把那个浏览器窗口临时抬到面板之上，被压住的那一条就露出来了。所以这条白线的高度**就是出血量**。

原来出血写的是 8px，量出来正好是 `120×9` 个像素、持续 12~25ms；现在压到 1px，只剩 2 行。
这 2 行已经是几何上的下限——窗口顶边只能是整数像素，而面板不透明区的下沿不是；
防缝也不再靠出血，改成窗口顶边**向下取整**（`Math.floor`），保证网页顶边永远不会落到顶栏不透明区的下沿以下。
改动前后的静态截图**逐像素完全一致**，也就是说除了那条白线变矮，画面上什么都没动。

顺手把"有窗口跑到面板之上"这件事也纳入了 30ms 看门狗：原来只靠前台事件钩子（13~30ms 内按回去）
和 200ms 的层级轮询兜底，钩子万一没装上就要错 200ms。

（要彻底消掉这条线，只能不让窗口浮上去，需要给浏览器窗口加 `WS_EX_NOACTIVATE` 再自己控制激活时机，
涉及键盘焦点，先不做。）

### 验证

新增 `scripts/verify-clip-pixels.js` 钉死"外壳入侵"这个回归。它给每个分格页面刷上**纯洋红**底，
再对着屏幕实拍，取"下面那格被裁区"与"上面那格可视区"的交集判定：横向取三行看整条，
**竖着逐像素扫三列**抓外框线和 ✕（判据是"凡不是洋红就是入侵物"）。覆盖 2 格 / 4 格、
"选中下面那格之后"、"把背板注入回去"和"把窗口样式注回去"看 30ms 看门狗认不认得出。

（两处判据的坑：横向那三行抓不到框线，因为框线只有 1~2px 高、采样行正好从它两行中间穿过去；
而一开始用洋红/亮绿棋盘格，"棋盘格两色的水平边界"在每一列都共线，会被当成窗框线误报，
换纯洋红后判据塌缩成"数非洋红像素"，误报源才消掉。）

脚本带**自证**：加 `AIQUAD_TEST_KEEP_BACKDROP=1` 跑时应用侧会故意不修，此时脚本必须报失败
（报绿就说明脚本瞎了）；另有 `AIQUAD_TEST_KEEP_FRAME=1` 可以只挂窗框（背板照常修），
专门用来单独校这条判据——不然两种入侵混在一起，量到的像素说明不了是哪一个。

顶栏那条白线由新加的 `scripts/verify-topbar-flash.js` 钉住，判据是"闪的那一帧有几行"，上限 2 行，
并自带负向对照（带 `AIQUAD_TEST_KEEP_FRAME=1 AIQUAD_TEST_KEEP_BACKDROP=1` 跑时必须量到整条 38 行，
量不到即判失败）。它没用原来那套 PowerShell 截图：那种路子一帧要 50ms（≈19Hz），
抓不到只存在 1~2 帧的东西，会得出"没有入侵"的假绿；改成一次 `BitBlt` 到自建的 DIB 段、
零拷贝成 `Uint32Array` 整块扫，实测 ≈162Hz。脚本开头还有一道"ROI 里顶栏底色占比"的自检，
把"取样取错 / 点击没落到分格里"这两种同样会给出假绿的情况挡在外面。

过程中还揪出两个"自检本身有 bug"的坑：注入的棋盘格一开始只改了 `html` / `body` 背景，
被页面自身的不透明容器盖住，于是"一行像素的极差"实际在量"这一段有没有文字"——白底黑字也给 255、
空白处给 0，跟背板毫无关系；以及看门狗新加的核对一开始写在"可见区核对"后面，
而那一段带 `continue`（区域正常是常态），等于永远不执行。两处都已修，并在代码里留了说明。

设置页侧的三条回归照常跑：页面自检（41 个 `id` 全在、16 个 `ⓘ` 齐、20 行、无行溢出，
窄窗 640px 回落单列且不溢出）+ 保存链路（`collect → save → 回读磁盘`）23 项全通过。


## [0.4.10] - 2026-09-15

修掉一个一直定位不准的老问题：2 格 / 4 格布局下选中下面那格，它会有一截"入侵"到上面那格的底部；顺带修掉点上面那两格时，顶栏底下会闪一下的那条白线。

### 修的问题

**分格里那截"被裁掉的浏览器外壳"其实有两层，都会漏到相邻分格上，而且两层都是 DWM 画的、都不受 `SetWindowRgn` 约束。** 分格里的浏览器是标准窗口，自带约 96px 的标题栏 + 标签栏 + 地址栏，我们靠 `SetWindowRgn` 把这一截从可视区裁掉，问题出在 Win11：

1. **系统背板（Mica）**：DWM 会给窗口铺一层背景，它照旧铺满整窗。Chrome 自己的内容确实被裁掉了，那一截却变成一条死板纯色（跟着壁纸走，浅色壁纸下是奶白 `#F9F1EB`），照样压在邻居上面。
2. **原生窗口框架**：窗口样式里的标题栏位还会让 DWM 在窗口矩形边上画一圈 1px 外框线和右上角一个 ✕（关闭按钮）。它面积很小，但同样越过了裁剪区域，在邻格上表现为"一条横线和一个 ✕"。

2 格 / 4 格布局里，"下面那格被裁掉的那一截"正好落在上面那格的底部，于是两层都露在那里。

这个 bug 之前几次都没修掉，是因为**它看不见**：`GetWindowRgn` / `PtInRegion` 读回来一切正常（区域确实设上了，命中测试也确实生效），所有基于区域的自检都判"通过"，而以前的自检全在读区域——**只有采屏幕像素才看得见**。之前还一直以为"本机是 Win10 + 软件渲染所以复现不出来"，其实本机就是 Win11 24H2（build 26100），能复现。

修法是两步，代价都约等于零：关掉系统背板（`DwmSetWindowAttribute` + `DWMWA_SYSTEMBACKDROP_TYPE = NONE`），以及摘掉窗口样式里的 `WS_CAPTION | WS_THICKFRAME`。除了建窗口和每次定位时各确认一次，30ms 几何看门狗也加了三项核对——Chrome 在激活 / 失活 / 换皮肤 / 刚显示出来时会把这些重新挂回来，而那时候几何往往纹丝不动，光靠"位置变了才修"兜不住。

（样式位是逐个实测定下来的：单独摘 `WS_THICKFRAME` 一点用没有，单独摘 `WS_CAPTION` 或 `WS_SYSMENU` 只减一半，两个一起摘才干净；摘掉之后窗口的外壳厚度会从 `{8,96,7,7}` 变成 `{6,96,7,6}`，看门狗会按新厚度重新定位。）

### 点上面那两格时，顶栏底下闪一下的那条白线

1 格 / 2 格（上面那格）/ 4 格（上排那两格）都一样：点一下，顶栏底边会冒出一条网页的白线，一两帧就没了。

原因是顶栏那一排的网页**故意**往上出血、压在顶栏底下（`--bleed-top`，为了顶栏和网页之间不留缝），而点分格时系统会把那个浏览器窗口临时抬到面板之上，被压住的那一条就露出来了。所以这条白线的高度**就是出血量**。

原来出血写的是 8px，量出来正好是 `120×9` 个像素、持续 12~25ms；现在压到 1px，只剩 2 行。这 2 行已经是几何上的下限——窗口顶边只能是整数像素，而面板不透明区的下沿不是；防缝也不再靠出血，改成窗口顶边**向下取整**（`Math.floor`），保证网页顶边永远不会落到顶栏不透明区的下沿以下。改动前后的静态截图**逐像素完全一致**，也就是说除了那条白线变矮，画面上什么都没动。

顺手把"有窗口跑到面板之上"这件事也纳入了 30ms 看门狗：原来只靠前台事件钩子（13~30ms 内按回去）和 200ms 的层级轮询兜底，钩子万一没装上就要错 200ms。

（要彻底消掉这条线，只能不让窗口浮上去，需要给浏览器窗口加 `WS_EX_NOACTIVATE` 再自己控制激活时机，涉及键盘焦点，先不做。）

### 验证

新增 `scripts/verify-clip-pixels.js` 钉死这个回归。它给每个分格页面刷上**纯洋红**底，再对着屏幕实拍，取"下面那格被裁区"与"上面那格可视区"的交集判定：横向取三行看整条，**竖着逐像素扫三列**抓外框线和 ✕（判据是"凡不是洋红就是入侵物"）。覆盖 2 格 / 4 格、"选中下面那格之后"、"把背板注入回去"和"把窗口样式注回去"看 30ms 看门狗认不认得出。

（两处判据的坑：横向那三行抓不到框线，因为框线只有 1~2px 高、采样行正好从它两行中间穿过去；而一开始用洋红/亮绿棋盘格，"棋盘格两色的水平边界"在每一列都共线，会被当成窗框线误报，换纯洋红后判据塌缩成"数非洋红像素"，误报源才消掉。）

脚本带**自证**：加 `AIQUAD_TEST_KEEP_BACKDROP=1` 跑时应用侧会故意不修，此时脚本必须报失败（报绿就说明脚本瞎了）；另有 `AIQUAD_TEST_KEEP_FRAME=1` 可以只挂窗框（背板照常修），专门用来单独校这条判据——不然两种入侵混在一起，量到的像素说明不了是哪一个。

顶栏那条白线由新加的 `scripts/verify-topbar-flash.js` 钉住，判据是"闪的那一帧有几行"，上限 2 行，并自带负向对照（带 `AIQUAD_TEST_KEEP_FRAME=1 AIQUAD_TEST_KEEP_BACKDROP=1` 跑时必须量到整条 38 行，量不到即判失败）。它没用原来那套 PowerShell 截图：那种路子一帧要 50ms（≈19Hz），抓不到只存在 1~2 帧的东西，会得出"没有入侵"的假绿；改成一次 `BitBlt` 到自建的 DIB 段、零拷贝成 `Uint32Array` 整块扫，实测 ≈162Hz。脚本开头还有一道"ROI 里顶栏底色占比"的自检，把"取样取错 / 点击没落到分格里"这两种同样会给出假绿的情况挡在外面。

过程中还揪出两个"自检本身有 bug"的坑：注入的棋盘格一开始只改了 `html` / `body` 背景，被页面自身的不透明容器盖住，于是"一行像素的极差"实际在量"这一段有没有文字"——白底黑字也给 255、空白处给 0，跟背板毫无关系；以及看门狗新加的核对一开始写在"可见区核对"后面，而那一段带 `continue`（区域正常是常态），等于永远不执行。两处都已修，并在代码里留了说明。

## [0.4.9] - 2026-09-15

这版主要是收拾稳定性：快捷键录制会卡住、退出时浏览器档案写不完整、顶栏偶发的闪白和分格底部露透明块。设置页的提示也精简了一遍。

### 修的问题

快捷键录制状态卡死。之前的状态栏只在「录制中」和「按键不合规」两种情况下写文字，回到空闲态什么都不做，所以按 Esc 取消、或者按了不合规的键之后放弃，那句「录制中…」或红色警告就永久留在页面上。现在空闲态会清空状态栏并重新校验一次。连带修掉一个并发坑：两轮校验同时跑时，晚返回的旧结论会盖掉新结论。

退出时浏览器档案可能写不完整。「后台分格休眠」用 `NtSuspendProcess` 把进程整个冻住，冻住的进程连消息泵都停了，`WM_CLOSE` 排在队列里没人处理，只能等 6 秒兜底强杀。现在退出前先统一解冻。实测：挂起状态下发关闭消息，2.5 秒后进程仍在；解冻后立刻正常退出。

顶栏闪白、分格 3 / 4 在上一格底部露透明块。原来的看门狗 250ms 只核对可见区的外接矩形，浏览器自己挪动窗口、或者重排外壳（顶部工具栏厚度变了）它都判为正常。换成 30ms 的完整几何核对：外壳厚度、窗口位置尺寸、可见区、以及挖洞是否还在，另外补上「该显示却被藏掉」的恢复。这个问题只在特定环境（Win11 + GPU 合成）复现，我们这边复现不出来，只能做成防御式核对。

「退出时清理缓存」一直等于没做。原来发完关闭消息就立刻同步删文件，浏览器进程还活着，文件删不掉，还会被它退出时写回去。现在会等浏览器进程真的结束再清理，并给「装更新时退出」留了旁路，免得拖住安装程序。

切布局时可能留下孤儿窗口。分格休眠在共享登录态、或分格还没就绪时会直接返回，什么也不做，窗口就留在屏幕上。现在一律降级为隐藏窗口。

配置项越界值直接落到正在跑的界面上。之前更新配置不做校验，要等下次启动才纠正，现在更新后立刻校验。

### 安全

「在浏览器里打开」现在只放行 http / https。之前是把任意字符串交给系统去挑默认程序，`file:`、`ms-msdt:` 这类同样照单全收。渲染层订阅 IPC 频道也改成了白名单。

### 其它

CDP 连接在缺少 `globalThis.WebSocket` 的运行时里会静默失败，现在显式报错并带上 Node 版本。切换「始终置顶」不再触发一次无谓的 200ms 滑动动画。清掉了几处死代码，前台事件钩子改成最后一个订阅者退订时自动卸载。

### 验证

100% 和 125% 两档缩放下跑完回归，全部通过：分格与浏览器窗口落位误差 0px，分层合成的鼠标穿透正确，下拉浮层挖洞正确，动画期间网页与面板逐帧跟随，面板回到最上层耗时 13 ~ 29ms，快捷键抓取 29 项、设置页 27 项、退出解冻 6 项、闲置清理 5 项。

## [0.4.8] - 2026-09-15

用**真实 Chrome / Edge 进程**驱动的多 AI 并排对话面板。这版主要按用户反馈补了设置页的几处缺失，顺手收拾了界面顶部的白条。

### 界面

顶栏高度从 30px 压到 26px，图标按钮改成 24×22 的无底色样式，横向 padding 和间距都收窄，把空间让给网页内容。

修掉了顶栏下沿那条白线。原因是顶栏到分格之间有两像素透出桌面，现在让第一行分格向上出血 8px 压到顶栏底下，顶栏再补一道同色投影兜底。分格圆角统一成 5px（之前主进程和样式表写的不是一个数，网页的直角会把面板画的圆角顶掉）。

### 新增

面板宽度加了预设按钮，20 / 30 / 40 / 50%，点一下直接生效，旁边会算出对应的实际像素宽度。保留原来的滑块。

AI 服务列表的操作栏多了「隐藏」。隐藏只是把它从分格底部的选择器里摘掉，已经绑在格子上的照常工作，配置也还留着，随时能放回去。要彻底删掉才用「删除」。

关于页补上了开源信息：许可证、仓库地址、反馈入口、用到的开源组件和它们的许可，以及一份隐私说明（浏览器档案、配置、代理设置全部留在本机，不上传）。

浏览器档案缓存可以清理了。设置页显示当前占用和分档案明细，手动点一下就清；也可以选退出时清理，或者攒到 500MB 时下次启动自动清。清的是 Cache / Code Cache / GPUCache 这类能再生的东西，Cookie 和 Local Storage 不动，登录态不会掉。

安装包多了便携版 `AIQuad-Portable-0.4.8.exe`，不写注册表，拷到哪里都能跑。设置页可以直接更新（基于 GitHub Releases），启动后静默查一次，之后每 6 小时复查。

### 修的问题

收纳后再呼出界面卡住。面板隐藏后 Chromium 会对它做重度节流，动画帧和定时器都被拖慢。已经关掉后台节流，收起后的归位定时器改成可取消（180ms 内再呼出不会被瞬移回停靠位），动画结束用刷新后的矩形重算一次鼠标穿透。

代理设置里点「测试连通性」一直卡在「测试中」。重写了测试逻辑：整体套硬超时，HTTPS 目标走 CONNECT 握手并校验返回码，支持 SOCKS5，能解析 Windows 那种带 `http=` 和 `https=` 前缀的多方案代理串，错误码给中文说明。现在不管通不通，几秒内一定出结果。

改面板宽度时不再触发整棵档案树的同步扫描，缓存统计也改成异步，拖滑块不会卡界面。

### 调整

分格切换的快捷键 `Ctrl+Alt+1 / 2 / 4` 默认不再绑。顶栏本来就有这三个按钮，绑了容易和其它软件抢键。已经装过旧版本的用户，如果这三个值从来没改过，升级后会自动清空；自己抓过的组合保持不动。呼出面板的 `Alt+Space` 不受影响。

依赖版本全部从 latest 改成波浪号范围，避免某天拉到一个大版本更新就编不过。

### 验证

100% 和 125% 两档缩放下跑完回归：分格与浏览器窗口落位误差 0px，分层合成的鼠标穿透正确，下拉浮层挖洞位置正确，拖动正常，动画期间网页与面板逐帧跟随（误差中位数 0px）。

## [0.4.6] - 2026-09-14

用**真实 Chrome / Edge 进程**驱动的多 AI 并排对话面板。这版主要收拾多显示器 / 非 100%
缩放环境下的窗口层级与动画问题。

### 本次修的问题

**面板与网页被拆成两层。** 之前面板恒置顶、浏览器窗口不置顶，于是别的程序能盖住网页、
盖不住面板，看上去就是「黑边浮在最上面，里面的网页被别的窗口压住」。现在两者放在同一个
置顶层，面板永远压在浏览器窗口之上。Windows 会按激活顺序重排同一层里的窗口（点一下网页，
那个窗口就会爬到面板上面），所以另外加了一个 200ms 一轮的 Z 序看门狗把次序按回去。

**收纳 / 弹出的动画有断层。** 原因是动画期间把浏览器窗口整个隐藏了，动完之后再显示，
网页和面板就对不上。改成不隐藏，动画每一帧都把新位置推给浏览器窗口，网页贴着面板一起走。

**动画卡。** 原来每帧都在跑全量几何重算（枚举浏览器子窗口、重设窗口区域、重排 settle
定时器），一帧算不完就掉帧。现在动画期间只做位置同步，帧间隔从 16ms 收到 8ms
（Windows 定时器精度约 15.6ms，按 16ms 排会掉到 30fps），动画时长 240ms 弹出 / 200ms 收起。

**托盘图标。** 之前一直没换，现在和窗口图标、设置页图标统一由 `scripts/make-icons.py`
从同一张 `LOGO.png` 生成（ico 七档 16~256，托盘 64px）。

### 自 v0.4.2 以来的其它变化

- **顶栏白条（Win11）**：原因是在 Win11 上带 `WS_EX_NOREDIRECTIONBITMAP` 的 Chromium 窗口，
  DWM 合成时会忽略 GDI 窗口区域，只对命中测试生效。改成面板透明 + 恒置顶的分层合成，
  视觉靠渲染层 alpha，鼠标穿透用 `setIgnoreMouseEvents` 按需开关。
  顺带确认了一条红线：**绝不给面板（Electron 窗口）设 `SetWindowRgn`**，屏幕缩放不是
  100% 时主进程会直接崩（退出码 `0xC0000409`）。
- **非 100% 缩放下的落位**：坐标一律换算成物理像素后再交给 `SetWindowPos`，
  之前只乘了一半，110% 缩放下浏览器窗口整体左移 171px，跑到面板外面。
- **UI 改版**：分格底部加了状态条，设置页、图标与整体配色重做。
- **AI 下拉浮层**：浮层超出分格的部分靠在浏览器窗口上「挖洞」让位，缩放环境下坐标同步修正。

### 验证

100% / 110% / 125% 三档缩放下跑完回归：分格与浏览器窗口落位误差 0px，分层合成的鼠标
穿透正确，动画期间网页与面板逐帧跟随（误差中位数 ≤ 2px），打包版实拍同样通过。

## [0.4.2] - 2026-09-14

用**真实 Chrome / Edge 进程**驱动的多 AI 并排对话面板。

不把 AI 网页塞进内嵌 webview，而是驱动真实的浏览器窗口——因此登录、二次验证、
Passkey（Windows Hello）全部原生可用，不会被站点判定为"应用内嵌浏览器"。

### 主要特性

- **1 / 2 / 4 分格同开**：每格独立选 AI（内置 ChatGPT / Gemini / Grok / Claude / Perplexity /
  DeepSeek / Kimi / 通义千问 / 豆包 / 元宝 / 智谱清言，可自定义添加）
- **登录态跨格共享**：一格登录，其它格切到同一站点免登录
- **快捷键呼出**：默认 `Alt+Space`；设置页里**直接按下组合键自动抓取**，不用手打字符串
- **分格内容逐像素对齐**：误差 0px，浏览器自带的标题栏 / 标签栏 / 地址栏不可见，
  分格内点击、键盘输入与中文输入法均正常
- **启动自愈**：Chromium GPU 崩溃、或浏览器升级换了安装目录后，自动降级 / 重新探测，
  不再弹错误框直接退出
- 托盘常驻、面板停靠屏幕左右、宽度 20%~60% 可调

### 运行环境

- 需要本机装有 **Google Chrome** 或 **Microsoft Edge**（Win10/11 自带 Edge，保底可用）
- 首次启动会创建一份浏览器档案（`%APPDATA%\aiquad\profiles\shared`）用于保存登录态

### 致谢

立项时以 [`rtugeek/ai`](https://github.com/rtugeek/ai) 为对照对象，分析其为什么会被
Google 判定为"应用内嵌浏览器"，从而选择了相反的路线。
**本项目未使用其任何源代码**，详见 README 的「致谢与参考」一节。
